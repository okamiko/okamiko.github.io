#include <stdio.h>
#include <iostream>
#include "ClassroomManager.h"
#include "Classroom.h"


int main() {
	// Classroom *myClass = new Classroom();
	// ClassroomManager *myClassManager = new ClassroomManager();

	// myClass->reserveSeat(257); // invalid checking
	// myClass->reserveSeat(-1);  // invalid checking
	// myClass->reserveSeat(100); 
	// myClass->reserveSeat(80);
	// std::cout << "\n" ;
	// myClass->checkSeatStatus(257); // invalid checking
	// myClass->checkSeatStatus(-1);  // invalid checking
	// myClass->checkSeatStatus(100);
	// myClass->freeSeat(80);
	// printf("Number of available seats: %d\n", myClass->getNumberOfFreeSeats());
	// std::cout << "\n" ;

	// std::cout <<"Reserved Room 5 Seat 200 \n";
	// myClassManager -> reserveSeatInRoom(5,200);
	// std::cout <<"Reserved Room 5 Seat 200 \n";
	// myClassManager -> checkSeatStatusOfRoom(5,250);

	// std::cout <<"skip line \n";
	// printf("Number of available seats: %d\n", myClassManager->getNumberOfFreeSeatsInRoom(5));

	// myClassManager->freeSeatInRoom(5,200); 

	// std::cout <<"skip line \n";
	// myClassManager->getStudentLocation(5);
}